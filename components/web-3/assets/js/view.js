/* replace with component lib */

(function( APP ){

	APP = APP || window.APP;

	var View = APP.Main3D.extend({

	});

	//APP.UI = APP.UI || {};
	//APP.UI.Component = View;
	Web3 = View;

})( this.APP );
